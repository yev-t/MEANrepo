import { NumberValidationDirective } from './number-validation.directive';

describe('NumberValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new NumberValidationDirective();
    expect(directive).toBeTruthy();
  });
});
